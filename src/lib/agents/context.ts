// DEPRECATED — demo StockContext blobs (hardcoded AAPL numbers) were
// only used for pipeline verification before the real data layer
// existed. The Sweep agent now fetches live cited facts via Claude
// web_search; we no longer need synthetic fixtures.
//
// CLAUDE.md: "no demo paths in production. no synthetic fallbacks."
// Safe to delete this file.
export {};
